export interface PochvenSignatureInput {
    sig: string
    type: string
    name: string
}

export interface PochvenSignatureOutput {
    sig: string
    type: string
    name: string
    systemId: number
    createdTime: string
}
